## Assignment 1

## - Goutham R, 1st Feb 2026  

### 1. Create a file and add executable permission to all users (user, group, and others)
Create a file and grant execute permission to the user, group, and others.

---

### 2. Create a file and remove write permission for the group user alone
Create a file and remove only the write permission for the group.

---

### 3. Create a file and add a soft link in a different directory
Create a file in `dir1/dir2/file` and create a symbolic (soft) link to that file inside `dir1`.

---

### 4. Display all active processes running on the system
Use the `ps` command with appropriate options to list all active processes.

---

### 5. Create files and redirect sorted output
Create three files in `dir1` and redirect the output of the `ls` command, sorted by timestamp, into a file.


